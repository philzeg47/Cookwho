# Rétrospective — Epic 1 : Socle & accès organisateur

_Date : 2026-06-22 · Facilitation : Amelia (Developer) · Participant : Philippe (Project Lead)_

## Bilan

- **Stories : 5/5 `done`** (1.1 init/thème, 1.2 composants UI, 1.3 connexion lien magique, 1.4 espace « Mes repas », 1.5 socle d'accessibilité).
- **Qualité :** 34 tests verts · lint/typecheck/build verts · revue de code (3 couches adversariales) → **0 bug bloquant**, 2 décisions + 10 patches appliqués, ~9 nitpicks écartés.
- **Process :** rien n'est encore committé depuis `f27b049`.
- Première rétrospective du projet (pas de précédente à recouper).

## Ce qui a bien fonctionné

- **L'accessibilité transformée en gate CI** : le test de contraste WCAG a attrapé un vrai défaut (Chip non-aimé à 3,24:1) — préfigure le corpus d'or sécurité de l'Epic 4.
- **Frontière `/core`** (règle ESLint posée en 1.1) tenue tout du long.
- **Décision `User` = organisateur** (pas de renommage du modèle Auth.js) : débloque proprement l'Epic 2.
- **Réutilisabilité du transport Resend** : posé en 1.3, prêt pour les invitations FR3.
- Discipline de documentation des écarts archi ↔ réel à chaque story.

## Défis / points d'amélioration

- **Écart archi ↔ réel récurrent** (Next 16→15, Tailwind v4, structure auth) — bien géré mais à garder en tête.
- **Friction de test next-auth sous Vitest** (`next/server`) — résolue (mock `~/server/auth` / assertions source), désormais en mémoire projet.
- **Dette du scaffold T3** (démo `Post`/Discord) traînée jusqu'à la revue de code avant nettoyage — à purger plus tôt à l'avenir.

## Action items & engagements

1. **[Process] Récap PM en fin de chaque story** (livré + à savoir, sans jargon). — appliqué dès l'Epic 2.
2. **[Process] Committer l'Epic 1** avant d'empiler l'Epic 2 (préserver le sens de `baseline_commit`).
3. **[Tech] Schéma Prisma métier** (`Repas`, `Participant`+`accessToken` 256 bits, `Restriction`) — story 2.1.
4. **[Tech] Réutiliser Resend** pour l'envoi des invitations (FR3).
5. **[Tech] Réappliquer le pattern de test next-auth/Vitest** pour les tests d'intégration des routers tRPC orga/participant.

## Découvertes significatives

- L'Epic 2 introduit le **premier schéma de données réel** → première **migration Prisma** + besoin d'une **DB Postgres** accessible. **Aucune** remise en cause du plan : l'architecture tient.

## Préparation Epic 2 — « Créer un repas & inviter »

Dépendances Epic 1 : ✅ auth/session · ✅ transport Resend (réutilisable FR3) · ✅ composants UI · ✅ espace `/repas`.
À construire : schéma métier, routers tRPC séparés orga/participant, route `/repas/creer`, `expiresAt` + purge cron (RGPD), token participant 256 bits.

## Readiness

- Stories : ✅ toutes `done`. Tests/qualité : ✅. Blocage : ❌ aucun.
- Manuel restant (hors agent) : vérifier le domaine Resend (DNS) avant prod ; provisionner une DB Postgres pour l'Epic 2 ; committer l'Epic 1.
